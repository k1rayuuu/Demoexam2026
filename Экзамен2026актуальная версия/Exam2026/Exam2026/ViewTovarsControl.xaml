<UserControl x:Class="Exam2026.ViewTovarsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:converters="clr-namespace:Exam2026.Converters"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Exam2026"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <converters:StrikeThroughConverter x:Key="StrikeThroughConverter"></converters:StrikeThroughConverter>
        <converters:SalePriceConverter x:Key="SalePriceConverter"></converters:SalePriceConverter>
    </UserControl.Resources>
    <Grid>

        <ListBox ItemsSource="{Binding Items}" BorderThickness="2" BorderBrush="Black"
                 HorizontalAlignment="Center">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid Width="500" Background="{Binding BackgroundColor}" Margin="20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="auto"></ColumnDefinition>
                            <ColumnDefinition Width="*"></ColumnDefinition>
                            <ColumnDefinition Width="auto"></ColumnDefinition>
                        </Grid.ColumnDefinitions>
                        <Border BorderBrush="Black" BorderThickness="2" Margin="5">

                            <Image Width="50" Height="50" Source="{Binding Фото}" ></Image>
                        </Border>
                        <Border Grid.Column="1" BorderBrush="Black" BorderThickness="2" Margin="5">

                            <StackPanel Orientation="Vertical" >

                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding Категория_товара}"/>
                                    <TextBlock Text=" | "/>
                                    <TextBlock Text="{Binding Наименование_товара}"/>
                                </StackPanel>

                                <TextBlock Text="{Binding Описание_товара}"/>
                                <TextBlock Text="{Binding Поставщик}"/>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="Цена: "></TextBlock>
                                    <TextBlock Text="{Binding Цена}" 
                                           TextDecorations="{Binding IsStrikeThrough, Converter={StaticResource StrikeThroughConverter}}"
                                           Foreground="{Binding IsStrikeThrough, Converter={StaticResource SalePriceConverter}}"/>
                                    <TextBlock Text="{Binding СкидочнаяЦена}" Foreground="Black"></TextBlock>
                                </StackPanel>
                                <TextBlock Text="{Binding Единица_измерения}"/>
                                <TextBlock Text="{Binding Кол_во_на_складе}"/>
                            </StackPanel>
                        </Border>
                        <Border Width="50" Height="50" Grid.Column="2" BorderBrush="Black" BorderThickness="2" Margin="5">
                            <TextBlock  Text="{Binding Действующая_скидка}" HorizontalAlignment="Center"></TextBlock>
                        </Border>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>
